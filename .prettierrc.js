module.exports = {
  // 每行最多多少个字符换行
  printWidth: 100,
  // 使用分号，默认true
  semi: true,
  // tab缩进大小，默认2
  tabWidth: 2,
  // 使用单引号，默认flase(jsx配置无效)
  singleQuote: true,
  // jsx使用单引号
  jsxSingleQuote: true,
};
